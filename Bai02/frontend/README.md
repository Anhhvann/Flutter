# Frontend Auth App

## Setup
1. Install dependencies:
	- flutter pub get
2. Run the app:
	- flutter run

## API Base URL
The API base URL is defined in:
- lib/services/api_client.dart

Default (Android emulator):
- http://10.0.2.2:3000

If you run on a real device, replace it with your machine IP.
